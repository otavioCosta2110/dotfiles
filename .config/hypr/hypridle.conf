general {
    # lock_cmd = pidof hyprlock || hyprlock
    # unlock_cmd = killall hyprlock
    before_sleep_cmd = qs -p /home/otavio/dev/noctalia-shell ipc call lockScreen toggle
    # after_sleep_cmd = hyprctl dispatch dpms on
    ignore_dbus_inhibit = true
    ignore_systemd_inhibit = true
}

listener {
    timeout = 300
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

listener {
    timeout = 310
    # on-timeout = qs -p /home/otavio/dev/noctalia-shell ipc call lockScreen toggle
}

listener {
    timeout = 1800
    # on-timeout = qs -p /home/otavio/dev/noctalia-shell ipc call sessionMenu lockAndSuspend
}
